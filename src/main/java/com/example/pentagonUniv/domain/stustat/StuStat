import java.util.Date;
import lombok.Data;

@Data
public class StuStat {
    private Long id;
    private Long studentId;
    private String status; // 재학, 휴학, 졸업 등
    private Date fromDate;
    private Date toDate; // null 이면 현재 상태
    private Long breakAppId; // 어떤 신청서로 바뀌었는지
}